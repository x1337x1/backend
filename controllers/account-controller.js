const db = require("../models");
const { } = require('./utils/account');

const accountsController = {

	async register(req, res) {
		try {

		} catch (error) {

		}
	},


};

module.exports = accountsController;